generator client {
  provider = "prisma-client"
  output   = "../src/repo/prisma-client"
}

datasource db {
  provider = "postgresql"
}

model Profile {
  userId   String @id @unique
  userName String @unique @db.VarChar(255)
}
